<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/cafe_claro" /> <!-- Color del fondo -->
    <!-- Color de fondo transparente -->
    <corners android:radius="30dp" />
    <stroke
        android:width="2dp"
        android:color="@color/cafe_claro" /><!-- Color del borde (rojo en este caso) -->
    />
</shape>